 # Repay M03 - 小鹏 MONA M03 Max 还款记录系统

 ## 项目简介

 记录小鹏 MONA M03 Max（2025 款 610km）购车款的个人还款明细。
 背景：车总价 147000 元，和对象各承担一半（73500 元/人）。对象全款买下，
 用户那 73500 元由对象垫付，用户分期还给她。目前剩余约 3.4 万元待还。
 本系统用于记录每笔还款（日期、金额、备注），并展示还款进度，不做计息。

 ## 样式规范

 系统视觉与交互样式统一参考 [.rule/design.md](./.rule/design.md)，
 design.md 基于 [.rule/ios_design.md](./.rule/ios_design.md)（iOS 毛玻璃设计系统）落地，
[.rule/adobe_design.md](./.rule/adobe_design.md) 为用户保留的原始设计参考。
 所有页面的配色、字体、间距、组件风格、布局规则、动效均以 design.md 为准；
 如与 Ant Design Vue 默认样式冲突，以 design.md 为准。

 ## 业务模型

 - 总欠款：73500 元（固定，无利息）
 - 当前剩余：约 34000 元
 - 已还：73500 - 34000 ≈ 39500 元
 - 还款方式：不定期、不固定金额，还清即结束
 - 不涉及银行贷款、利息、等额本息等计算
 - 面向用户：用户本人和对象，两人都可能记账

 ## 账户系统

 固定三个账户，账户名不可改，昵称可改，密码可改：

 | 账户 | 初始昵称 | 初始密码 | 说明 |
 |------|---------|---------|------|
 | admin | 管理员 | admin123 | 初始化数据操作人用 admin |
 | xrz | 仙人 | admin123 | 用户本人 |
 | ds | 大帅 | admin123 | 对象 |

 - 登录后所有操作记录当前登录人
 - 账户名不可编辑，昵称可编辑，密码可编辑

 ## 功能清单

 ### 记账核心
 - 新增还款记录：日期 + 金额 + 备注（前两项必填）
 - 任意记录可编辑、删除（含历史合计记录）
 - 快速录入：默认日期填今天，金额输入框自动聚焦
 - 金额校验：不能为负，不能超过剩余欠款
 - 备注常用词快捷输入：工资上缴、微信转账、支付宝、现金等，点选即填，也可自由输入

 ### 概览与进度
 - 核心数字：总欠款、已还、剩余、还清进度百分比
 - 进度条可视化
 - 还款进度可推演（无固定周期，按实际记录计算）
 - 里程碑提醒：还过半、还剩 1 万以下、全部还清等节点触发提示

 ### 明细列表
 - 支持正序/倒序切换，默认倒序（最新在前）
 - 按月分组显示，月小计
 - 备注模糊搜索
 - 按月份筛选
 - 每条记录显示：日期、金额、备注、记录人、操作（编辑/删除）

 ### 历史明细
 - 历史合计作为一条正常还款记录录入，备注标注"历史合计"及日期
 - 用户自行决定何时删除合计记录、替换为逐笔明细
 - 不设独立入口，历史明细补录走正常"新增还款记录"流程

 ### 数据管理
 - 数据存储使用轻量化数据库（技术设计见下文"数据存储技术设计"）
 - 支持导出/导入备份
 - 导入校验

 ### 不需要的功能
 - 预算
 - 多币种、汇率
 - 周期性记账模板
 - 多账户体系（只有固定三个登录账户）
 - 分类/标签树

 ## 技术栈

 - **框架**: Vite + Vue 3 + JavaScript（非 TypeScript）
 - **UI 库**: Ant Design Vue
 - **状态**: Vue 3 reactive / composable（轻量，无需 Pinia）
 - **数据持久化**: 轻量化数据库（见下文技术设计）
 - **日期处理**: date-fns
 - **样式**: 基于 Adobe Spectrum 设计令牌，覆盖 AntDV 默认样式
 - **动效**: 仪式感动效（见 design.md 动效章节）

 ## 数据存储技术设计

 采用 **IndexedDB + Dexie.js** 方案：

 - **为何不用纯 JSON 文件**：浏览器端无法安全写入本地文件；localStorage 仅同步、容量小且无索引能力；
   纯 JSON 内存态在多记录、增删改查、模糊搜索时性能与安全性都不足。
 - **Dexie.js** 是 IndexedDB 的轻量封装，提供 Promise API、索引声明、事务支持，
   适合中等规模结构化数据，且天然支持模糊查询（via filter + 索引）、按字段排序与分组。
 - **表结构**：
   - `users`：主键 `username`，字段 `nickname`、`password`（明文，个人记账场景可接受）
   - `payments`：主键自增 `id`，索引 `date`、`createdBy`，字段 `amount`（元）、`note`、`createdAt`
 - **初始化（seed）**：首次启动若库为空，写入 3 个固定账户（admin/xrz/ds，密码均 admin123），
   以及一条历史合计还款记录（金额 39500，备注「历史合计 2025-08」，操作人 admin）。
 - **导入/导出**：导出为 JSON 文件（含 users + payments 全量）；导入时做结构校验与主键去重，
   覆盖式写入并记录操作人。用于跨设备迁移。
 - **跨设备说明**：当前为单机本地库；未来如迁移到手机端，可复用同一 Dexie schema，
   通过导入/导出 JSON 在设备间迁移数据。

 ## 项目结构

 ```
 src/
   main.js                  # 应用入口
   App.vue                  # 根组件
   router/index.js          # 路由（含登录守卫）
   stores/
     auth.js                # 登录状态、当前用户
     repay.js               # 还款数据状态
   db/
     database.js            # 数据库初始化与连接
     schema.js              # 表结构定义
     dao.js                 # 数据访问层（增删改查）
   utils/
     format.js              # 金额、日期格式化
     validator.js           # 金额校验等
   views/
     Login.vue              # 登录页
     Dashboard.vue          # 概览（总额/已还/剩余 + 进度 + 里程碑）
     Payments.vue           # 还款明细列表（排序/筛选/搜索/增删改）
     Settings.vue           # 设置（改密码/改昵称/导入导出）
   components/
     StatCard.vue           # 统计卡片
     PaymentModal.vue       # 新增/编辑还款弹窗
     ProgressOverview.vue   # 进度可视化
     MilestoneToast.vue     # 里程碑提示
 ```

 ## 开发约定

 - 金额以元为单位存储（Number），展示时保留两位小数
 - 日期统一使用 YYYY-MM-DD 格式
 - 中文 UI，代码注释用中文，变量名用英文
 - 提交信息用中文
 - 所有数据操作记录操作人（登录账户名）
 - 动效须尊重 prefers-reduced-motion

 ## 运行方式

 ```bash
 npm install      # 安装依赖
 npm run dev      # 启动开发服务器
 npm run build    # 构建生产版本
 ```

 ## 待确认事宜

 - 数据存储方案选型（见下文展开）
 - 初始化数据：先录一条"历史已还合计 39500 元"，备注标注日期，操作人 admin
 - 仪式感动效细节确认（见 design.md）
